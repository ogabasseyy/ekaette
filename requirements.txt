# Ekaette — Python Dependencies
# Pin versions for reproducible builds

# Core framework
google-adk>=1.25.1
google-genai>=1.14.0
google-cloud-aiplatform>=1.82.0
fastapi>=0.131.0
uvicorn[standard]>=0.34.0
websockets>=15.0

# Google Cloud services
google-cloud-firestore>=2.22.0
google-cloud-storage>=2.19.0

# Environment
python-dotenv>=1.1.0

# Testing (dev only — also in pyproject.toml if we add one)
pytest>=8.3.0
pytest-asyncio>=0.25.0
pytest-cov>=6.1.0
httpx>=0.28.0
